---
title: Dostawcy, modele i agenci
description: Połącz dostawców AI, wybierz modele i korzystaj z wbudowanego agenta Pi.
---

# Dostawcy, modele i agenci

Zanim OpenChamber cokolwiek zrobi, potrzebuje co najmniej jednego połączonego dostawcy AI. Ta strona opisuje łączenie dostawcy, wybór modelu i działanie wbudowanego agenta Pi.

## Połącz dostawcę

1. Otwórz **Settings → Providers**.
2. Otwórz menu **Add provider** i wybierz dostawcę, który nie jest jeszcze połączony.
3. Zaloguj się na jeden z dwóch sposobów, zależnie od dostawcy:
   - **Klucz API** — wklej swój klucz i zapisz.
   - **Logowanie (device flow)** — OpenChamber pokazuje link i krótki kod. Otwórz link, wpisz kod i zatwierdź. OpenChamber sam dokończy łączenie.

Gdy dostawca pokazuje się jako połączony, jego modele stają się dostępne w czacie.

Aby się rozłączyć, otwórz dostawcę i wybierz usunięcie jego logowania.

## Wybierz model

Model wybierasz tam, gdzie pracujesz:

- w czacie użyj selektora modelu w pasku wiadomości, aby ustawić dostawcę i model dla tej sesji
- w **Settings → Session Defaults** ustaw domyślny model i thinking

## Wbudowany agent

Pichamber używa jednego wbudowanego agenta (`pi`). Na Pi **Settings → Agents** jest ukryte. Nie tworzysz tam dodatkowych agentów ani osobowości.

Zamiast tego użyj:

- **Model i thinking** — pasek wiadomości w czacie albo **Settings → Session Defaults**
- **Plan** — tryb sesji na tym samym agencie. Zainstaluj i włącz go w **Settings → Feature Plugins**. Kompozytor pokaże wtedy **Agent / Plan**.
- **Subagents** — dodatkowi pracownicy przez Feature Plugins **Subagents** i `/run`, nie przez dodatkowych agentów w Settings

Na pozostałym jądrze OpenCode (`OPENCHAMBER_KERNEL=opencode`) **Settings → Agents** nadal służy do tworzenia i edycji nazwanych agentów.

## Gdzie są przechowywane Twoje logowania

Logowania dostawców są przechowywane przez OpenCode, a nie OpenChamber, więc są współdzielone z OpenCode CLI. Jeśli ustawisz tego samego dostawcę w więcej niż jednym miejscu, wygrywa ustawienie najbardziej szczegółowe: ustawienie dla projektu nadpisuje Twoje osobiste.

## Powiązane

- [Serwery MCP](/pl/mcp/) — dodaj agentom dodatkowe narzędzia
- [Zużycie i limity](/pl/usage/) — śledź, ile już wykorzystałeś
