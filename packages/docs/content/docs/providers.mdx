---
title: Providers, Models & Agents
description: Connect AI providers, pick models, and use the built-in Pi agent.
---

# Providers, Models & Agents

Before OpenChamber can do anything, it needs at least one AI provider connected. This page covers connecting a provider, choosing a model, and how the built-in Pi agent works.

## Connect a provider

1. Open **Settings → Providers**.
2. Open the **Add provider** menu and pick a provider that isn't connected yet, or choose **Other / Custom** for an OpenAI-compatible endpoint.
   **xAI / Grok** is in that list before you sign in. If every catalog provider is already connected, Add opens **Other / Custom** so you can still add another.
3. Sign in one of two ways, depending on the provider:
   - **Sign-in (device flow)** — OpenChamber shows a link and a short code. Open the link, enter the code, and approve. OpenChamber finishes connecting on its own. For xAI this is listed first: SuperGrok or X Premium. The token is stored in `~/.pi/agent/auth.json` and Pi refreshes it.
   - **API key** — paste your key and save. For xAI this is below SuperGrok.

xAI subscription allowance, cycle, and expiry appear on **Work Status** and the xAI card only after you install **Settings → Feature Plugins → Grok Usage**. You can sign in and chat without that plugin. Pichamber does not open the leftover **Settings → Usage** page for Grok.

### Custom / Other providers

For gateways, campus LLMs, Ollama, LiteLLM, and similar OpenAI-compatible APIs:

1. Choose **Other / Custom** in the provider list.
2. Enter a provider ID, display name, base URL (`http://` or `https://`), API key (or `{env:VAR_NAME}`), and at least one model id/name.
3. Optionally add request headers.
4. Save — OpenChamber writes the provider block to OpenCode config and stores the key in OpenCode auth (literal keys) or records an `{env:VAR}` reference.
5. To change an existing custom provider, open it and choose **Edit**.

Custom providers require an API key or `{env:VAR_NAME}` before they show as fully connected. Without credentials, models may appear but chat calls will fail.

When a provider shows as connected, its models become available in chat.

To disconnect, open the provider and choose to remove its sign-in.

## Pick a model

You pick the model where you're working:

- in chat, use the model picker in the message bar to set the provider and model for that session
- in **Settings → Session Defaults**, set the default model and thinking

## The built-in agent

Pichamber uses one built-in agent (`pi`). **Settings → Agents** is hidden on Pi. You do not create extra agents or personalities there.

Use these instead:

- **Model and thinking** — chat message bar, or **Settings → Session Defaults**
- **Plan** — a session mode on the same agent. Install and enable it under **Settings → Feature Plugins**. The composer then shows **Agent / Plan**.
- **Subagents** — extra workers via Feature Plugins **Subagents** and `/run`, not extra Settings agents

On the leftover OpenCode kernel (`OPENCHAMBER_KERNEL=opencode`), **Settings → Agents** is still where you create and edit named agents.

## Where your sign-ins live

Provider sign-ins are stored in `~/.pi/agent/auth.json`, shared with the Pi CLI. If you set the same provider in more than one place, the most specific setting wins: a per-project setting overrides your personal one.

## Related

- [MCP Servers](/mcp/) — add extra tools for agents
- [Usage & Quotas](/usage/) — track how much you've used
